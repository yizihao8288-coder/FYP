# Tests

- 这是什么：事件构造、数据源审计、样本质量、输出完整性和控制系统测试。
- 为什么存在：把“我记得代码应该这样工作”变成可重复检查。
- 输入：代码、配置和本机现有冻结数据。
- 输出：通过/失败状态，不产生论文结论。
- 可修改：新增功能必须补测试；不得为了让失败消失而放宽冻结hash。
- 验证：运行`run.ps1 -Test`；所有临时文件必须位于D盘。
- 当前状态：`validated`。
